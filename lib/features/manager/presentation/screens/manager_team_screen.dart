import 'package:flutter/material.dart';

class ManagerTeamScreen extends StatelessWidget {
  const ManagerTeamScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('My Team'),
      ),
      body: const Center(
        child: Text('Manager Team Management list - Work in Progress'),
      ),
    );
  }
}
